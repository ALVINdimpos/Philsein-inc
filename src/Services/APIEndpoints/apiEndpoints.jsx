// services/apiEndpoints.js
const BASE_URL = 'https://api.example.com'

export const ENDPOINTS = {
  LOGIN: `${BASE_URL}/login`,
  USER: `${BASE_URL}/users`,
  PRODUCTS: `${BASE_URL}/products`,
}

// ... other API-related constants or functions
